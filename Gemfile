source 'https://rubygems.org'

gem 'github-pages', '~> 232', group: :jekyll_plugins
gem 'commonmarker'

# Standard library gems that were removed from Ruby's default gem set.
# Required for forward-compatibility with Ruby 3.4+ even though Ruby 3.3.x
# (used by GitHub Pages) still ships them as defaults.
gem 'base64'
gem 'logger'
gem 'csv'
